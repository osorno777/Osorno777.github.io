# Claude / Cursor local agent

Cursor reads this file on every chat. Follow `AGENTS.md` in this folder and `tools/translation_qa/AGENTS.md`.

Secrets are in `tools/translation_qa/.env` (gitignored), not here. See `tools/translation_qa/HANDOFF_LOCAL_AGENT.md`.
